# ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".

name: release-prod
on:
  workflow_dispatch:
    inputs:
      version:
        description: Package version
        required: true
jobs:
  deploy:
    name: Release stage prod to AWS
    needs: []
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
      packages: read
    env:
      CI: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: AWS Credentials
        uses: aws-actions/configure-aws-credentials@master
        with:
          role-to-assume: arn:aws:iam::002630180209:role/GitHub-event-system
          role-session-name: GitHubAction
          aws-region: eu-central-1
      - run: echo "GITHUB_TOKEN=${{ secrets.GITHUB_TOKEN }}" >> $GITHUB_ENV
      - run: npx projen install:ci
      - run: yarn add @aws-community-dach/event-system-backend@${{github.event.inputs.version}}
      - run: mv ./node_modules/@aws-community-dach/event-system-backend cdk.out
      - run: npx projen deploy:prod
      - uses: actions/upload-artifact@v3
        with:
          name: cdk-outputs-prod
          path: cdk-outputs-prod.json
